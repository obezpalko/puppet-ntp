# automatically generated by puppet
# do not change
#
listen on *

<% if @ntp_servers == '' %>
servers 0.ubuntu.pool.ntp.org
servers 1.ubuntu.pool.ntp.org
servers 2.ubuntu.pool.ntp.org
servers 3.ubuntu.pool.ntp.org
<% else %>
<% @ntp_servers.each do |server| %>servers <%= server %>
<% end %>
<% end %>
